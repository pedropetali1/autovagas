{
  "project": "autovagas",
  "branchName": "ralph/scheduled-pipeline",
  "description": "Pipeline Agendada por Usuário — cada usuário define dias da semana e horário de execução automática do pipeline de busca de vagas. Cron global horário verifica quem deve rodar. Botão manual mantido com cota por plano. Inclui correções de incoerências na codebase.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Adicionar campos de agendamento ao modelo de dados",
      "description": "As a developer, I need to persist the user's scheduled days and time so the cron can check who should run at each hour.",
      "acceptanceCriteria": [
        "Adicionar campo `scheduleDays Int[]` ao modelo `User` em `prisma/schema.prisma` com default `[1,2,3,4,5]` (1=Seg, 2=Ter, 3=Qua, 4=Qui, 5=Sex, 6=Sáb, 0=Dom)",
        "Adicionar campo `scheduleTime String?` ao modelo `User` (formato HH:00, ex: '08:00'), nullable",
        "Gerar migration com `npx prisma migrate dev --name add-schedule-fields` e aplicar com sucesso",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Criar helper de timezone BRT centralizado",
      "description": "As a developer, I need a single utility for BRT timezone calculations to eliminate duplicated logic between cron.ts and application.ts.",
      "acceptanceCriteria": [
        "Criar arquivo `lib/timezone.ts` exportando função `getCurrentBRTTime(): { hourStr: string, dayOfWeek: number, todayStartUTC: Date }` onde hourStr é 'HH:00', dayOfWeek é 0-6 (0=Dom), e todayStartUTC é 00:00 BRT em UTC",
        "Usar `Intl.DateTimeFormat` ou cálculo manual (UTC-3) sem bibliotecas externas",
        "Substituir o cálculo inline de BRT em `workers/cron.ts` pelo helper `getCurrentBRTTime()`",
        "Substituir o cálculo inline de BRT em `server/routers/application.ts` (query de quota diária) pelo campo `todayStartUTC` do helper",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Substituir cron fixo por cron horário com verificação de agenda",
      "description": "As a developer, I need to replace the fixed daily cron with an hourly cron that dispatches only users whose schedule matches the current hour.",
      "acceptanceCriteria": [
        "Em `workers/cron.ts`, remover o job repetível antigo com `queue.removeRepeatable('daily-pipeline', { cron: '0 11 * * *' })` antes de registrar o novo",
        "Trocar cron expression para `0 * * * *` (toda hora em ponto)",
        "Na seleção de usuários, filtrar apenas onde: `scheduleTime` não é null, `scheduleDays` contém o dayOfWeek atual (BRT), e `scheduleTime === hourStr` atual (BRT) — usando `getCurrentBRTTime()` de `lib/timezone.ts`",
        "Manter todos os filtros existentes: `cvUrl` not null, `automationPaused = false`, skills >= 3, quota diária não atingida",
        "Usuários FREE sem `scheduleTime` definido não são enfileirados (pipeline só roda se agendamento estiver configurado)",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Endpoint TRPC para salvar configuração de agendamento",
      "description": "As a user, I want to save my preferred schedule so the system knows when to run automatically.",
      "acceptanceCriteria": [
        "Criar mutation `updateSchedule` em `server/routers/user.ts`",
        "Input Zod: `{ scheduleDays: z.array(z.number().int().min(0).max(6)), scheduleTime: z.string().regex(/^\\d{2}:00$/).nullable() }`",
        "Persistir via `prisma.user.update` nos campos `scheduleDays` e `scheduleTime`",
        "Retornar o user atualizado com os novos campos",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Controle de cota para execução manual por plano",
      "description": "As a user on a paid plan, I want to trigger the pipeline manually at any time, while free users are limited to once per day.",
      "acceptanceCriteria": [
        "Em `server/routers/application.ts`, na mutation `triggerPipeline`: se `user.plan === 'FREE'`, manter verificação de `lastPipelineRunAt` (1x/dia); se `user.plan === 'PLUS'` ou `'PRO'`, pular a verificação de tempo e sempre permitir",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Extrair constante PLANS para fonte única",
      "description": "As a developer, I need a single source of truth for plan definitions to avoid divergence between the billing router and landing page.",
      "acceptanceCriteria": [
        "Criar `lib/plans.ts` exportando constante `PLANS` com os três planos (FREE/PLUS/PRO) incluindo id, name, price, dailyQuota e features",
        "Remover definição local de `PLANS` (ou equivalente) de `app/page.tsx` e importar de `lib/plans.ts`",
        "Remover definição local de `PLANS` (ou equivalente) de `server/routers/billing.ts` e importar de `lib/plans.ts`",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Corrigir lógica duplicada de matching de skills",
      "description": "As a developer, I need to eliminate duplicated skill-matching code in the matching worker.",
      "acceptanceCriteria": [
        "Em `workers/matching.ts`, criar função local `calculateSkillMatch(userSkills: string[], requirements: string[]): { matched: string[], missed: string[], score: number }`",
        "A função retorna `score` como `(matched.length / requirements.length) * 100` (ou 100 se requirements estiver vazio)",
        "Substituir as duas ocorrências duplicadas do cálculo de skillsMatched/skillsMissed pela chamada a `calculateSkillMatch`",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Adicionar limite de paginação em ApplicationLog",
      "description": "As a developer, I need to cap the ApplicationLog query to prevent slow responses for applications with many log entries.",
      "acceptanceCriteria": [
        "Em `server/routers/application.ts`, na query `getLogs`, adicionar `take: 100` e `orderBy: { createdAt: 'desc' }` ao `prisma.applicationLog.findMany`",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Adicionar middleware de autenticação de rotas",
      "description": "As a developer, I need a Next.js middleware to redirect unauthenticated users who try to access protected routes directly.",
      "acceptanceCriteria": [
        "Criar `middleware.ts` na raiz do projeto (mesmo nível de `package.json`)",
        "Rotas protegidas: `/dashboard`, `/vagas`, `/perfil`, `/planos` — redirecionar para `/sign-in` se não houver sessão Supabase válida",
        "Usar `createServerClient` do `@supabase/ssr` para verificar a sessão, seguindo o padrão de `lib/supabase-server.ts`",
        "Rotas públicas (`/`, `/sign-in`, `/sign-up`, `/api/**`) não são interceptadas",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Corrigir falha silenciosa no envio de e-mails",
      "description": "As a developer, I need email send failures to be logged so we can detect and diagnose notification issues.",
      "acceptanceCriteria": [
        "Em `lib/email.ts`, no bloco `catch`, adicionar `console.error('[email] Failed to send:', template, error)` antes de retornar",
        "Em `workers/apply.ts`, substituir todas as ocorrências de `void sendEmail(...)` por `await sendEmail(...).catch(err => console.error('[apply] email failed:', err))`",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "UI de agendamento no Dashboard",
      "description": "As a user, I want to configure my automation schedule from the dashboard so I can control when the system searches for jobs.",
      "acceptanceCriteria": [
        "Adicionar seção 'Agendamento' em `app/dashboard/page.tsx` imediatamente abaixo do card de toggle de automação",
        "Exibir 7 botões toggle compactos para dias da semana (Dom/Seg/Ter/Qua/Qui/Sex/Sáb), multi-seleção, com fundo `#1a1a1a`, borda `#2a2a2a`, e cor de seleção ativa `#b5ff4e`",
        "Exibir `<input type='time' step='3600'>` para escolha da hora (apenas horas cheias)",
        "Pré-popular os campos com valores do perfil do usuário; se `scheduleTime` for null, usar padrão `scheduleDays=[1,3,5]` e `scheduleTime='08:00'` como estado local inicial (não persiste até salvar)",
        "Exibir badge 'Próxima execução: [dia], [data] às [hora]' calculado no frontend com base nos valores do formulário",
        "Botão 'Salvar agendamento' chama mutation `user.updateSchedule` com os valores atuais do formulário",
        "Exibir toast de sucesso ('Agendamento salvo!') ou erro após a mutation",
        "Todos os controles da seção ficam com opacidade 0.5 e `pointer-events: none` quando `automationPaused = true`",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Tooltip diferenciado no botão de busca manual por plano",
      "description": "As a user, I want to understand the manual search limitations based on my plan.",
      "acceptanceCriteria": [
        "No dashboard, o botão 'Buscar Vagas' exibe tooltip 'Busca manual (1x por dia)' para plano FREE e 'Buscar vagas agora' para planos PLUS/PRO",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Adicionar error boundaries nas páginas protegidas",
      "description": "As a user, I should see a friendly error message instead of a crashed blank page if something fails.",
      "acceptanceCriteria": [
        "Criar `app/dashboard/error.tsx` com componente de error boundary do Next.js App Router ('use client', props: `{ error: Error, reset: () => void }`)",
        "Criar `app/vagas/error.tsx` com o mesmo padrão",
        "Criar `app/perfil/error.tsx` com o mesmo padrão",
        "Cada error.tsx exibe: mensagem 'Algo deu errado. Tente recarregar a página.' e botão 'Recarregar' que chama `reset()`",
        "Estilo dark theme consistente: fundo `#111`, texto `#e5e5e5`, botão com accent `#b5ff4e`",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 13,
      "passes": false,
      "notes": ""
    }
  ]
}
