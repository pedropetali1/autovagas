## Codebase Patterns
- Supabase Storage: use service role key client in lib/supabase.ts; `createSignedUploadUrl(path, { upsert: true })` for overwrite-safe presigned upload URLs; `getPublicUrl(path)` for public read URL
- tRPC v11 + Next.js App Router: use `fetchRequestHandler` from `@trpc/server/adapters/fetch` in `app/api/trpc/[trpc]/route.ts`
- Clerk v7 auth in tRPC context: `const { userId: clerkId } = await auth()` from `@clerk/nextjs/server`
- tRPC protectedProcedure loads full user from Prisma and adds to ctx; access as `ctx.user` in procedure handlers
- tRPC frontend client: `createTRPCReact<AppRouter>()` in `lib/trpc.tsx`; Provider setup in `app/providers.tsx` with `httpBatchLink({ url: '/api/trpc', transformer: superjson })` + `ClerkProvider` + `QueryClientProvider`
- Sync server state to controlled form: use a `synced` boolean flag to copy query data into local state once on load, avoiding re-initialization on every render
- Prisma 7 has breaking changes: no `url` in datasource block; URL goes in `prisma.config.ts`
- `binaries.prisma.sh` (Prisma CDN) is unreachable in this environment; use custom schema engine
- Custom schema engine (`scripts/schema-engine.mjs`) handles all Prisma RPC + CLI calls
- The fake engine binary at `node_modules/@prisma/engines/schema-engine-debian-openssl-3.0.x` delegates to `schema-engine.mjs`
- SQL generation uses `@prisma/prisma-schema-wasm` `get_dmmf` with `{ prismaSchema: content }` param
- `prisma migrate dev` flow: devDiagnostic → applyMigrations(existing) → evaluateDataLoss → createMigration → applyMigrations(new)
- `createMigration` response uses `extension: 'sql'` (no leading dot) — WIe writes `migration.${extension}`
- `applyMigrations` response key is `appliedMigrationNames` (camelCase)
- PRISMA_SCHEMA_ENGINE_BINARY env var only affects CLI mode; daemon uses the binary from node_modules/@prisma/engines
- Schema-engine `@@index` regex is greedy — only captures LAST `@@index` per model; manually add missing indexes to migration SQL after generation
- Schema engine generates full CREATE TABLE/INDEX for all models each migration (not delta); `applyMigrations` ignores DDL errors on re-run
- Prisma: never mix `include` and `select` at the same query level — use only `select` with nested `select` for relations
- Cursor pagination: `take: limit + 1`, then `items.pop()` to get nextCursor; skip: 1 when cursor provided
- Stripe SDK: use `apiVersion: '2026-02-25.clover'` for stripe@20.4.1 (latest as of 2026-03)

# Ralph Progress Log
Started: Mon Mar 23 11:54:39 PDT 2026
---

## 2026-03-23 - US-001
- Implemented Prisma schema: all 4 enums (Plan, ApplicationStatus, ApplyType, LogAction), models User, Skill, Job, Application (placeholder)
- Created `prisma.config.ts` for Prisma 7 datasource URL config
- Built pure-JS schema engine stub `autovagas/scripts/schema-engine.mjs` using @prisma/prisma-schema-wasm + PGlite
- Created wrapper binary at node_modules/@prisma/engines/schema-engine-debian-openssl-3.0.x
- `prisma migrate dev --name init` succeeded; migration SQL generated in prisma/migrations/
- TypeScript typecheck passes
- Files changed: prisma/schema.prisma, prisma.config.ts, .env, scripts/schema-engine.mjs, scripts/schema-engine.sh, node_modules/@prisma/engines/schema-engine-debian-openssl-3.0.x
- **Learnings for future iterations:**
  - Prisma 7 WASM schema engine (`@prisma/schema-engine-wasm`) crashes with "Option::unwrap_throw()" — use pure-JS stub instead
  - Use `@prisma/prisma-schema-wasm`'s `get_dmmf({ prismaSchema: content })` for DMMF extraction (not `get_dmmf({ datamodel: ... })`)
  - `@@index` is NOT in DMMF; parse from schema text with regex instead
  - `global.PRISMA_WASM_PANIC_REGISTRY = { set_message: () => {} }` required before requiring prisma-schema-wasm CJS module
  - `excludeCompanies String[] @default([])` field: DMMF default is `[]` (array), generate `DEFAULT ARRAY[]::TEXT[]`
---

## 2026-03-23 - US-002
- Replaced placeholder Application model with full definition: matchScore, appliedAt, errorLog, failReason, directUrl, createdAt, updatedAt, logs relation
- Added @@unique([userId, jobId]), @@index([userId, status]), @@index([createdAt])
- Added ApplicationLog model: applicationId, action, detail (Json), screenshotUrl, createdAt; cascade delete from Application
- Added @@index([applicationId]), @@index([createdAt]) to ApplicationLog
- `prisma migrate dev` succeeded; Prisma client regenerated; typecheck passes
- Files changed: prisma/schema.prisma, prisma/migrations/20260323125833_migration/migration.sql
- **Learnings for future iterations:**
  - Schema-engine @@index regex is greedy and only captures the LAST @@index per model — manually add missing indexes to migration SQL after running prisma migrate dev
  - After schema changes, run `npx prisma generate` to regenerate the Prisma client (types won't update otherwise)
---

## 2026-03-23 - US-003
- Created Prisma client singleton at `autovagas/lib/prisma.ts` (global cache pattern for dev hot reload)
- Created route handler `autovagas/app/api/webhooks/clerk/route.ts` — POST /api/webhooks/clerk
- Validates Svix signature using svix npm package + CLERK_WEBHOOK_SECRET env var
- On user.created: upserts User with clerkId, email, name from payload
- On user.updated: updates email and name in User record
- Returns 200 on success, 400 on invalid/missing signature, 500 on DB error
- Installed svix npm package
- Files changed: autovagas/lib/prisma.ts, autovagas/app/api/webhooks/clerk/route.ts, autovagas/package.json, autovagas/package-lock.json
- **Learnings for future iterations:**
  - Prisma client singleton goes in `lib/prisma.ts`; use `globalThis` cast for dev hot-reload safety
  - `.env` is gitignored — add new env vars there locally but do NOT commit
  - Next.js App Router route handlers: `export async function POST(request: Request)` in `app/.../route.ts`
  - Svix headers are `svix-id`, `svix-timestamp`, `svix-signature` (lowercase)
---

## 2026-03-23 - US-004
- Installed @trpc/server@11, @trpc/client, @trpc/react-query, @tanstack/react-query, zod, @clerk/nextjs@7, superjson
- Created `server/trpc.ts`: createTRPCContext using Clerk auth(), protectedProcedure enforces auth and loads User from DB
- Created `server/routers/user.ts` with 9 procedures: getProfile, updateProfile, addSkill, removeSkill, updateNotificationPrefs, toggleAutomation, addExcludeCompany, removeExcludeCompany
- Created `server/routers/_app.ts` as root AppRouter
- Created `app/api/trpc/[trpc]/route.ts` using fetchRequestHandler from @trpc/server/adapters/fetch
- Typecheck passes
- Files changed: server/trpc.ts, server/routers/user.ts, server/routers/_app.ts, app/api/trpc/[trpc]/route.ts, package.json, package-lock.json
- **Learnings for future iterations:**
  - tRPC v11: `initTRPC.context<Context>().create({ transformer: superjson })` pattern
  - Clerk v7 `auth()` is async in server contexts; returns `{ userId }` (clerkId)
  - Prisma `excludeCompanies` array: use `{ push: value }` to append, `{ set: filteredArray }` to remove
  - `@clerk/nextjs/server` exports: `auth` (App Router), `currentUser`, `clerkMiddleware`
---

## 2026-03-23 - US-005
- Created `server/routers/application.ts` with 4 procedures
- `application.list`: cursor-based pagination (limit+1 pattern), optional ApplicationStatus filter, includes Job (title, company, location, salary, applyType), ordered by createdAt desc
- `application.getStats`: parallel Promise.all with aggregate (_count.id, _avg.matchScore) + 3 separate count queries for sent/failed/viewed
- `application.getTimeline`: last 10 applications with select (id, status, matchScore, appliedAt, job.title, job.company)
- `application.getLogs`: verifies ownership via userId check, returns ApplicationLog ordered by createdAt asc; throws NOT_FOUND or FORBIDDEN
- Registered applicationRouter in `server/routers/_app.ts`
- Typecheck passes
- Files changed: server/routers/application.ts, server/routers/_app.ts
- **Learnings for future iterations:**
  - Prisma: do NOT use both `include` and `select` at the same level — use only `select` with nested `select` for relations
  - Cursor pagination pattern: `take: limit + 1`, pop last item for nextCursor
  - Ownership verification pattern: `findUnique` with `select: { userId: true }`, compare with `ctx.user.id`
---

## 2026-03-23 - US-006
- Installed stripe@20.4.1
- Created `server/routers/billing.ts` with 3 procedures: getPlans (static 3-plan array), createCheckoutSession (Stripe Checkout Session), getSubscription (user plan + dailyQuota)
- Created `app/api/webhooks/stripe/route.ts`: validates Stripe signature with STRIPE_WEBHOOK_SECRET, handles checkout.session.completed (updates plan/dailyQuota via priceId→PLAN_CONFIG map) and customer.subscription.deleted (resets to FREE/1)
- Registered billingRouter in `server/routers/_app.ts`
- Files changed: server/routers/billing.ts, server/routers/_app.ts, app/api/webhooks/stripe/route.ts, package.json, package-lock.json
- **Learnings for future iterations:**
  - Stripe API version for stripe@20.4.1 is `'2026-02-25.clover'` — do NOT use `'2025-01-27.acacia'` or older versions
  - Stripe webhook handler: use `request.text()` (not `.json()`) before constructEvent() — body must be raw string
  - Plan→priceId mapping comes from env vars (STRIPE_PRICE_PLUS, STRIPE_PRICE_PRO); PLAN_CONFIG maps priceId→{plan, dailyQuota}
  - userId is stored in checkout session metadata at creation time; subscription.metadata?.userId for deletion events
---

## 2026-03-23 - US-007
- Installed @supabase/supabase-js
- Created `autovagas/lib/supabase.ts`: Supabase service-role client singleton with comment about bucket RLS policy requirement
- Added `user.getUploadUrl` mutation: calls `supabase.storage.from('cvs').createSignedUploadUrl('{userId}/cv.pdf', { upsert: true })` — returns { signedUrl, token, path }
- Added `user.confirmCvUpload` mutation: calls `supabase.storage.from('cvs').getPublicUrl('{userId}/cv.pdf')` and updates User.cvUrl
- Files changed: autovagas/lib/supabase.ts, autovagas/server/routers/user.ts, autovagas/package.json, autovagas/package-lock.json
- **Learnings for future iterations:**
  - Supabase `createSignedUploadUrl` returns `{ signedUrl, token, path }` — client uses `signedUrl` with PUT request or Supabase JS `uploadToSignedUrl`
  - Pass `{ upsert: true }` to `createSignedUploadUrl` so re-uploading the same path overwrites the file
  - The bucket 'cvs' RLS policy is a Supabase dashboard config, not code: `(auth.uid()::text = storage.foldername(name)[1])` restricts direct client access to the file owner's folder
  - Server-side operations use service role key so they bypass RLS by design
---

## 2026-03-23 - US-008
- Created `lib/trpc.tsx`: tRPC React client with `createTRPCReact<AppRouter>()`
- Created `app/providers.tsx`: ClerkProvider + QueryClientProvider + trpc.Provider using httpBatchLink + superjson
- Updated `app/layout.tsx`: wrap children with `<Providers>`; updated metadata to AutoVagas / pt-BR
- Created `app/perfil/page.tsx`: full profile page
  - Personal data form: name, phone, zipCode, linkedinUrl, desiredRole, minSalary; saves via user.updateProfile; toast 'Perfil salvo'
  - Skills section: removable tag chips + add-skill input; warning banner when <3 skills
  - CV upload: file input (PDF only, max 5MB); getUploadUrl → PUT to signedUrl → confirmCvUpload; shows current cv.pdf link
  - Skeleton loaders for all sections while query loading
- Typecheck passes
- Files changed: lib/trpc.tsx, app/providers.tsx, app/layout.tsx, app/perfil/page.tsx
- **Learnings for future iterations:**
  - tRPC React client setup: `createTRPCReact<AppRouter>()` in lib/trpc.tsx; provider in app/providers.tsx
  - ClerkProvider must wrap tRPC provider (and vice versa) — nest as `<ClerkProvider><trpc.Provider><QueryClientProvider>`
  - For CV upload: GET signedUrl from tRPC, then PUT file directly to Supabase Storage signedUrl
  - Use a `synced` boolean flag to sync server data into controlled form state once on load
---

## 2026-03-23 - US-009
- Added Notificações section to `/perfil` page with 3 Toggle components (emailDigest, emailOnViewed, emailOnFailed); saves via user.updateNotificationPrefs on each toggle click
- Added Filtros section with minSalary field (saves via user.updateProfile on form submit) and exclude-companies input with removable tag chips (addExcludeCompany/removeExcludeCompany)
- Extracted reusable `Toggle` component (accessible button with role="switch" and aria-checked)
- Moved minSalary from the personal data form to the Filtros section per AC
- All sections show skeleton loaders while query is loading
- Files changed: autovagas/app/perfil/page.tsx
- **Learnings for future iterations:**
  - Toggle (switch) pattern: use `<button role="switch" aria-checked={checked}>` for accessibility
  - Notification prefs are saved immediately on toggle change (no save button needed) — optimistic UI via local state update first, then mutation
  - excludeCompanies array is managed server-side via push/set; client reflects via query invalidation
---

## 2026-03-23 - US-010
- Created `autovagas/app/dashboard/page.tsx` with full dashboard implementation
- Greeting with user name (split on space for first name)
- PlanCard: plan name (FREE/PLUS/PRO → labels), dailyQuota, todayCount from getStats
- 4 MetricCards: total, sent, fail rate % (failed/total*100), avgScore%
- AutomationToggle: optimistic local state toggle, amber track + badge when paused, amber banner in page header when paused
- Timeline: application.getTimeline, StatusBadge per status, formatted timestamp via Intl.DateTimeFormat pt-BR
- Extended `server/routers/application.ts` getStats to include todayCount (createdAt >= today 00:00 local time)
- All sections use skeleton loaders while queries load
- Files changed: autovagas/app/dashboard/page.tsx, autovagas/server/routers/application.ts
- **Learnings for future iterations:**
  - Optimistic UI for toggle: save `localPaused` state in useState, update on onMutate, sync on onSuccess/onError
  - `new Date(); setHours(0,0,0,0)` gives start of today in local time — use for "today count" queries
  - Status badge pattern: Record<string, string> maps for labels and Tailwind color classes
  - Intl.DateTimeFormat('pt-BR', { day, month, year, hour, minute }) for formatted Brazilian timestamps
---

## 2026-03-23 - US-011
- Created `autovagas/app/vagas/page.tsx` with full vagas listing page
- Paginated list via `trpc.application.list.useInfiniteQuery` (limit=20, cursor-based); "Carregar mais" load-more button when `hasNextPage`
- Status filter dropdown: Todas / Pendente / Candidatando / Enviada / Falhou / Visualizada (maps empty string → undefined for tRPC)
- ApplicationCard: job title, company, location, salary (Intl.NumberFormat BRL), ApplyTypeBadge (EASY_APPLY=blue, EXTERNAL=gray), StatusBadge, MatchScoreBar
- MatchScoreBar: color-coded fill bar + percentage (green≥70%, amber 40-69%, red<40%)
- 'Candidatar manualmente' link (opens in new tab) only when status=FAILED AND failReason=FORM_NOT_SUPPORTED AND directUrl present
- Automation paused yellow banner at top when profile.automationPaused=true
- Skeleton loaders while isLoading
- Files changed: autovagas/app/vagas/page.tsx
- **Learnings for future iterations:**
  - `trpc.application.list.useInfiniteQuery` takes input WITHOUT cursor field; tRPC passes cursor automatically as pageParam
  - `data?.pages.flatMap(p => p.items)` to flatten infinite query pages into a single array
  - `hasNextPage` and `fetchNextPage` / `isFetchingNextPage` are destructured from useInfiniteQuery result
  - For conditional enum filter: use `(statusFilter as string) || undefined` to convert empty string to undefined for optional tRPC input
---

## 2026-03-23 - US-012
- Made ApplicationCard clickable (role="button", onClick/onKeyDown) to open detail modal
- Added ApplicationDetailModal: fixed overlay with backdrop, panel with header (job title, company, close ×), meta row (StatusBadge, matchScore %, appliedAt), FORM_NOT_SUPPORTED amber notice with 'Candidatar manualmente' link
- Added LogTimeline component: calls trpc.application.getLogs.useQuery, skeleton while loading, vertical timeline with connecting lines
- Log entries: emoji icon per LogAction (SCRAPE🔍, MATCH⚡, FILL_FORM✏️, SUBMIT📤, SCREENSHOT📸), pt-BR formatted timestamp, DetailKeyValue renders detail JSON as key-value pairs
- SCREENSHOT entries show clickable thumbnail (<img>) that opens screenshotUrl in new tab
- Added appliedAt: Date | null to ApplicationItem type (available from list query since it returns all Application columns)
- Files changed: autovagas/app/vagas/page.tsx
- **Learnings for future iterations:**
  - Modal pattern: fixed inset-0 z-50 flex, backdrop div with onClick=onClose, panel div as relative child
  - To stop the "Candidatar manualmente" link from also triggering the card's onClick, use e.stopPropagation() on the link's onClick
  - Prisma ApplicationLog.detail is typed as Prisma.JsonValue (unknown at runtime); cast to Record<string, unknown> for rendering
  - Vertical timeline with connecting lines: flex-col items, w-px flex-1 bg-gray-200 connector between items (skip on last)
  - appliedAt comes from application.list query since it uses include (returns all columns), not select
---

## 2026-03-23 - US-013
- Created `autovagas/app/planos/page.tsx`: 3 plan cards (Free/Plus/Pro) sourced from billing.getPlans
- billing.getSubscription used to highlight current plan with 'Seu plano' badge
- Plus/Pro 'Assinar' button calls billing.createCheckoutSession mutation and router.push(url) to Stripe Checkout
- Free and current plan cards show disabled 'Plano atual' button
- Updated `autovagas/app/dashboard/page.tsx`: added useSearchParams + useEffect to detect ?checkout=success query param on Stripe redirect success; shows toast 'Plano atualizado com sucesso' then clears param via router.replace('/dashboard')
- Downgraded users see quota reflected on next load (handled by existing Stripe webhook + billing.getSubscription query)
- Files changed: autovagas/app/planos/page.tsx (created), autovagas/app/dashboard/page.tsx
- **Learnings for future iterations:**
  - Stripe success redirect pattern: detect `?checkout=success` query param in useEffect on mount; show toast then router.replace to clean URL
  - useSearchParams() from 'next/navigation' works in 'use client' components to read query params
  - For plan card UI: grid-cols-3 with Pro card using dark (gray-900) background for visual hierarchy
  - Disable 'Assinar' button while mutation is pending using `createCheckoutSession.isPending`
---

## 2026-03-23 - US-014
- Replaced placeholder app/page.tsx with full SSG landing page
- Hero section: Playfair Display headline + DM Sans body font + 'Começar grátis' CTA
- Como funciona section: 3 illustrated steps with SVG icons (Busca vagas, Analisa compatibilidade, Candidata automaticamente)
- Stats section: 3 stats cards (10.000+ candidaturas, 50.000+ vagas/dia, 2.500+ usuários)
- Pricing section: hardcoded PLANS array (mirrors billing.ts PLANS) rendered as static plan cards with /sign-up links
- Final CTA section with Clerk /sign-up link
- Nav with Entrar (/sign-in) and Começar grátis (/sign-up) links
- Design system: #fafaf8 background, border #e8e7e0, DM Sans body, Playfair Display headings
- Both fonts loaded via next/font/google with CSS variables (--font-playfair, --font-dm-sans)
- Page is SSG by default (no dynamic data, no 'use client' directive)
- Files changed: autovagas/app/page.tsx
- **Learnings for future iterations:**
  - SSG in Next.js App Router: just don't use 'use client' or dynamic data — no need for generateStaticParams or export const dynamic = 'force-static'
  - next/font/google: load multiple fonts with separate calls, pass `.variable` to className to expose CSS variable, then reference via style prop or Tailwind
  - Static pricing section on landing: hardcode PLANS array (same data as billing router) — don't call tRPC from SSG page
  - Clerk signup/signin routes: /sign-up and /sign-in (configured via Clerk middleware)
---
---

## 2026-03-24 - US-015
- Created `autovagas/workers/scraper.ts`: BullMQ Worker consuming 'scraper' queue
- Playwright browser context with randomized userAgent, viewport (1280-1920px), locale (pt-BR), timezone (America/Sao_Paulo)
- Optional PROXY_URL env var routing via parseProxyUrl helper
- Rate limit: 500ms delay between requests = max 2 req/s per session
- Challenge detection: HTTP 429, /checkpoint, /authwall, /captcha URL patterns
- Exponential backoff: 30s → 60s → 120s with proxy rotation (new browser context per attempt) across 3 attempts
- After 3 failed Playwright attempts: fallback to LinkedIn Jobs Guest API HTML scraper (regex-based card parsing)
- Jobs upserted by linkedinUrl (dedup); applyType set by Easy Apply button detection
- Application records created with PENDING status (upsert to avoid duplication)
- SCRAPE ApplicationLog created per collected job
- Enqueues 'matching' job with { userId, jobIds[] } on completion
- Exports scraperQueue + matchingQueue for use by cron worker (US-019)
- Files changed: autovagas/workers/scraper.ts (created)
- **Learnings for future iterations:**
  - Workers live in `autovagas/workers/` directory; TypeScript `**/*.ts` glob includes them
  - BullMQ Queue + Worker pattern: create Queue for each queue name, Worker with connection config
  - Playwright proxy: pass `{ server, username, password }` to `chromium.launch({ proxy })` — each attempt creates fresh browser to get new proxy session
  - Challenge detection must check both response status AND final URL (after redirect) for authwall/captcha
  - LinkedIn guest API endpoint: `https://www.linkedin.com/jobs-guest/jobs/api/seeMoreJobPostings/search?keywords=...`
  - Application SCRAPE upsert: always upsert Application before creating ApplicationLog (need applicationId)
  - Export queues from worker module so cron (US-019) can enqueue without reimporting queue config
---

## 2026-03-24 - US-016
- Created `autovagas/workers/matching.ts`: BullMQ Worker consuming 'matching' queue
- Loads user with skills via prisma.user.findUnique with include: { skills: true }
- Excludes companies from User.excludeCompanies[] (case-insensitive toLowerCase comparison)
- Excludes jobs where job.salary < user.minSalary (only when both are non-null)
- Calculates matchScore = (skillsMatched / requirements.length) × 100; 100% when no requirements
- Filters out matchScore < 40
- Sorts remaining jobs by matchScore desc, selects top N = User.dailyQuota
- Upserts Application records (update matchScore if exists, create as PENDING if new)
- Creates MATCH ApplicationLog with detail { score, skillsMatched: string[], skillsMissed: string[] } for ALL evaluated jobs (not just selected ones)
- Exports applyQueue; enqueues 'apply' job with { applicationIds[] } on completion
- Files changed: autovagas/workers/matching.ts (created)
- **Learnings for future iterations:**
  - Matching worker receives jobIds from scraper worker; both use separate queue instances with same Redis connection config
  - matchScore calculation: use string includes for fuzzy skill matching (skill in requirement OR requirement in skill)
  - skillsMissed = requirements where no user skill matches (for MATCH log detail)
  - When job.requirements is empty, default matchScore to 100 (no requirements = open to all)
  - Export applyQueue from matching.ts so apply worker (US-017/018) and cron can reference it
---

## 2026-03-24 - US-017
- Created `autovagas/workers/apply.ts`: BullMQ Worker consuming 'apply' queue
- Filters to EASY_APPLY applications only (EXTERNAL will be handled by US-018)
- Skips already-settled applications (SENT or FAILED status)
- Sets status → APPLYING and creates FILL_FORM ApplicationLog before Playwright attempt
- Playwright browser context with randomized userAgent, pt-BR locale, Sao_Paulo timezone, optional PROXY_URL
- Navigates to job.linkedinUrl; detects captcha/authwall redirects
- Clicks Easy Apply button via multiple selector fallbacks (aria-label, text content)
- Fills name/email/phone fields using label/name/id/aria-label selectors
- Downloads CV from user.cvUrl via fetch and uploads to file input via setInputFiles()
- Takes screenshot, uploads to Supabase Storage at screenshots/{applicationId}/final.png, creates SCREENSHOT ApplicationLog
- Submits via multiple button selector fallbacks
- On success: SENT + appliedAt = now() + SUBMIT log { result: 'success' }
- On CAPTCHA: FAILED + failReason='CAPTCHA' + SUBMIT log { reason: 'CAPTCHA' }
- On timeout: FAILED + failReason='TIMEOUT' + SUBMIT log { reason: 'TIMEOUT' }
- Files changed: autovagas/workers/apply.ts (created)
- **Learnings for future iterations:**
  - Easy Apply button has many selector variations — use multiple fallbacks with try/catch
  - CV download from public URL then setInputFiles() with Buffer is the Playwright pattern for file upload
  - Apply worker concurrency=1 to avoid LinkedIn rate limiting (sequential applications)
  - Screenshot is taken after filling form but before submission (captures final filled state)
  - Always check for challenge redirects both before AND after submission
---
